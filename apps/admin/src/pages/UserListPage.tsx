/* ============================================================
 * P0-7 · 用户管理占位页（P4 阶段细化）
 * ============================================================ */

import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function UserListPage() {
  return (
    <div>
      <Title level={2}>用户管理</Title>
      <Card>
        <p>用户管理将在后续阶段实现：读者列表 / 作者列表 / 角色权限。</p>
      </Card>
    </div>
  );
}
