/* ============================================================
 * 兼容层：userStore 重定向到 atlasStore
 * ============================================================ */
export { useAtlasStore as useUserStore } from "./atlasStore";