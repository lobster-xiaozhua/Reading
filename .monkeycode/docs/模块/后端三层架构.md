# 后端三层架构

## 概述

后端采用经典的三层架构：API 路由层 → 服务层 → 仓储层，配合 ORM 模型层实现数据持久化。API 接口列表见 [INTERFACES.md](../INTERFACES.md)，核心概念见 [状态机](../专有概念/状态机.md)、[双Token鉴权](../专有概念/双Token鉴权.md)、[敏感词系统](../专有概念/敏感词系统.md)。

## 架构分层

```
┌─────────────────────────────────────────────────────┐
│  API 路由层 (api/)                                   │
│  ├── c_end/         C 端读者接口                     │
│  ├── b_end/         B 端管理接口                     │
│  └── deps.py        依赖注入                         │
├─────────────────────────────────────────────────────┤
│  服务层 (services/)                                  │
│  ├── book_service.py        书籍服务                 │
│  ├── discovery_service.py   发现页服务               │
│  ├── novel_service.py       作品管理服务             │
│  ├── chapter_service.py     章节管理服务             │
│  ├── auth_service.py        鉴权服务                 │
│  ├── interaction_service.py 互动服务                 │
│  ├── audit_service.py       审核服务                 │
│  ├── role_service.py        角色权限服务             │
│  ├── royalty_service.py     稿费服务                 │
│  ├── user_center_service.py 用户中心服务             │
│  ├── search_service.py      搜索服务                 │
│  ├── notes_service.py       笔记服务                 │
│  ├── sensitive_service.py   敏感词服务               │
│  ├── user_service.py        用户管理服务             │
│  ├── system_service.py      系统配置服务             │
│  ├── workbench_service.py   工作台服务               │
│  └── chart_service.py       图表服务                 │
├─────────────────────────────────────────────────────┤
│  仓储层 (repositories/)                              │
│  ├── base.py                泛型仓储基类              │
│  ├── novel_repo.py          作品仓储                  │
│  ├── chapter_repo.py        章节仓储                  │
│  ├── reader_repo.py         读者/书架/阅读历史仓储     │
│  ├── interaction_repo.py    互动仓储                  │
│  ├── audit_repo.py          审核仓储                  │
│  ├── role_repo.py           角色权限仓储              │
│  ├── royalty_repo.py        稿费仓储                  │
│  └── system_config_repo.py  系统配置仓储              │
├─────────────────────────────────────────────────────┤
│  ORM 模型层 (models/)                                │
│  ├── base.py                基类 + Mixin             │
│  ├── novel.py               作品/章节/标签/分类/Banner│
│  ├── user.py                读者/作者/管理员          │
│  ├── permission.py          角色/权限                 │
│  ├── reading.py             书架/阅读历史/统计        │
│  ├── interaction.py         评论/书评/打赏            │
│  ├── audit.py               审核/敏感词               │
│  ├── royalty.py             稿费                     │
│  ├── notes.py               笔记                     │
│  └── system_config.py       系统配置                 │
└─────────────────────────────────────────────────────┘
```

## 核心组件

### 配置管理

文件: `backend/app/core/config.py`

使用 `pydantic-settings` 的 `BaseSettings`，支持 `.env` 文件覆盖。关键配置：

- `db_url`: 数据库连接（默认 SQLite，生产用 MySQL）
- `redis_url`: Redis 连接
- `jwt_secret`: JWT 密钥
- `access_token_ttl`: Access Token 有效期（默认 8h）

### 数据库

文件: `backend/app/core/database.py`

- 异步引擎: `create_async_engine`
- 会话工厂: `AsyncSessionLocal`
- SQLite 默认（开发），MySQL 生产（asyncmy 驱动）
- 启动时自动 `create_all` 建表

### 异常体系

文件: `backend/app/core/exceptions.py`

```python
class BizError(Exception):
    def __init__(self, code: int, message: str, http_status: int = 200):
        self.code = code
        self.message = message
        self.http_status = http_status
```

### 缓存策略

- Redis 缓存发现页、书籍详情、排行榜等热点数据
- Cache-Aside 模式：先读缓存 → 未命中查数据库 → 写入缓存
- 开发/测试时 Redis 不可用自动降级为 fakeredis

## 数据流示例

以「获取书籍详情」为例：

```
GET /api/v1/c/books/123
    ↓
BookService.get_book(book_id=123, reader_id=1001)
    ↓
检查 Redis 缓存 → 命中则直接返回
    ↓
未命中 → NovelRepository.get_by_id(123)
    ↓
SQLAlchemy 查询 → ORM → 返回 Novel 模型
    ↓
_converters.novel_to_c_summary(novel, is_in_shelf)
    ↓
写入 Redis 缓存
    ↓
返回 BookSummary Schema
```

## 测试

- 框架: pytest + pytest-asyncio + httpx.AsyncClient
- 数据库: 内存 SQLite（`sqlite+aiosqlite:///:memory:`）
- Redis: fakeredis 模拟
- 共 162 个测试用例，覆盖 11 个测试文件