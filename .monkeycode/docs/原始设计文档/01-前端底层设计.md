# 前端底层设计规范

> **文档版本：V1.0.0 · 2026.07**
> 统一 C 端（Atlas Design System）与 B 端（Ant Design）底层令牌体系，作为小说网站全端 UI 的唯一基准。

---

## 目录

1. [文档定位](#1-文档定位)
2. [设计原则](#2-设计原则)
3. [Token 三层架构](#3-token-三层架构)
4. [色彩系统](#4-色彩系统)
5. [字体排印](#5-字体排印)
6. [间距系统](#6-间距系统)
7. [圆角系统](#7-圆角系统)
8. [阴影与层级](#8-阴影与层级)
9. [栅格布局](#9-栅格布局)
10. [动效规范](#10-动效规范)
11. [层级系统（z-index）](#11-层级系统z-index)
12. [暗黑模式架构](#12-暗黑模式架构)
13. [令牌输出格式](#13-令牌输出格式)
14. [无障碍基础](#14-无障碍基础)
15. [命名规范](#15-命名规范)

---

## 1. 文档定位

| 维度 | 说明 |
| --- | --- |
| 文档类型 | 前端底层设计规范（Design Tokens & Foundations） |
| 目标平台 | Web（C 端阅读站 + B 端创作者后台）、H5、轻量 PWA |
| 技术栈 | HTML5 / CSS Custom Properties / TypeScript / React；样式层支持原生 CSS 与 Tailwind 映射 |
| 令牌标准 | W3C Design Tokens Community Group（DTCG）2025.10 规范格式 [Research-backed] |
| 受众 | 前端工程师、设计工程师、UI/UX 设计师、QA、跨团队协作 PM |
| 覆盖范围 | 色彩、字体、间距、圆角、阴影、栅格、动效、层级、暗黑模式、无障碍、命名 |
| 不覆盖范围 | 组件库 API 细节、业务交互流程、品牌视觉指南（另行成册） |

**统一来源说明**

本规范基于两份源文档综合整理，并在冲突点做出统一裁决：

| 维度 | C 端 Atlas | B 端 Ant Design | 统一裁决 |
| --- | --- | --- | --- |
| 主品牌色 | #245BFF（钴蓝） | #1890FF | #1890FF 为主品牌色；#245BFF 为 C 端强调色（accent） |
| 中性色级数 | 14 级 | 9 级 | 统一 14 级（gray-1 ~ gray-14） |
| Token 层数 | 三层 | 两层 | 统一三层（Primitive → Semantic → Component） |
| UI 字体 | Noto Sans SC | PingFang SC | Noto Sans SC（跨平台可获取，覆盖率优于 PingFang） |
| 间距档数 | 13 档 | 6 档 | 统一 13 档（4px 基准） |
| 圆角档数 | 8 档 | 4 档 | 统一 8 档 |

---

## 2. 设计原则

1. **以人为本**：所有视觉决策服务于阅读体验与创作者效率。小说正文优先保证可读性（serif、18px、行高 1.8）；B 端优先保证信息密度与操作确定性。 [Research-backed]
2. **一致即信任**：同一语义（如"主要操作"）在全端只对应一个令牌（`--color-brand`）。用户跨端切换时无需重新学习。 [Research-backed]
3. **克制与留白**：用间距与中性色构建层次，而非堆砌色彩与阴影。强调色只用于真正需要吸引注意力的元素。 [Expert judgment]
4. **可度量**：一切数值来自令牌表，禁止裸值。间距 4px 基准、字号阶梯、阴影级数均可被工具校验。 [Research-backed]
5. **包容可达**：默认满足 WCAG 2.2 AA。文本对比度 ≥ 4.5:1，交互组件 ≥ 3:1，且不依赖单一色彩传达状态。 [Research-backed]
6. **动而有节**：动效服务于反馈与空间连续性，遵循 Doherty 阈值（≤ 100ms 感知为即时）。非必要不动画，并尊重 `prefers-reduced-motion`。 [Research-backed]

---

## 3. Token 三层架构

### 3.1 三层定义

| 层级 | 名称 | 职责 | 示例 | 可否被组件直接引用 |
| --- | --- | --- | --- | --- |
| L1 | **Primitive（原始令牌）** | 不可变的原子值，全局唯一真值 | `--gray-7: #7A7A86` | 否（仅 Semantic 引用） |
| L2 | **Semantic（语义令牌）** | 按用途命名，引用 Primitive，随主题切换 | `--color-text-secondary: var(--gray-8)` | 是（推荐） |
| L3 | **Component（组件令牌）** | 组件级别名，引用 Semantic | `--button-color-text: var(--color-text-inverse)` | 是（组件内部） |

> **核心铁律**：组件代码只允许引用 L2 语义令牌或 L3 组件令牌，**禁止直接引用 L1 原始令牌或裸色值**。 [Expert judgment]

### 3.2 核心链路图

```
┌─────────────────────────────────────────────────────────────────┐
│  L1 Primitive（原子值，不随主题变）                              │
│  --brand-7:#1890FF  --gray-14:#0A0B12  --gray-1:#F7F7F8  ...     │
└───────────────┬───────────────────────────────────┬─────────────┘
                │ var() 引用                         │ var() 引用
                ▼                                    ▼
┌───────────────────────────────────────┐  ┌─────────────────────────────────┐
│  L2 Semantic（语义层，随主题切换）     │  │  L2 Semantic（主题切换时重指向）  │
│  浅色: --color-text-primary:           │  │  暗色: --color-text-primary:      │
│         var(--gray-14)                 │  │         var(--gray-1)             │
└───────────────┬───────────────────────┘  └─────────────────────────────────┘
                │ var() 引用
                ▼
┌─────────────────────────────────────────────────────────────────┐
│  L3 Component（组件别名）                                        │
│  --button-text-primary: var(--color-text-inverse)               │
│  --button-bg-primary:   var(--color-brand)                      │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 代码示例

```css
/* L1 Primitive */
:root {
  --brand-7: #1890ff;
  --gray-14: #0a0b12;
}

/* L2 Semantic */
:root {
  --color-brand: var(--brand-7);
  --color-text-primary: var(--gray-14);
}

/* L3 Component */
.btn--primary {
  --button-bg-primary: var(--color-brand);
  --button-text-primary: var(--color-text-inverse);
  background: var(--button-bg-primary);
  color: var(--button-text-primary);
}

/* 主题切换：只改 L2 指向，L1 与 L3 不动 */
[data-theme='dark'] {
  --color-text-primary: var(--gray-1);
}
```

---

## 4. 色彩系统

### 4.1 品牌主色（brand，10 级） + C 端强调色（accent，10 级）

brand 色阶以 `brand-7 = #1890FF` 为基准（即 Ant Design 调色板 blue-6 的值，[Research-backed]），其下为浅色调（背景/底纹），其上为深色调（hover/active）。accent 色阶以 `accent-7 = #245BFF`（Atlas 钴蓝）为 C 端强调基准。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--brand-1` | `#F0F9FF` | 最浅底纹 / 选中态极淡背景 |
| `--brand-2` | `#E6F7FF` | 品牌浅底纹、tag 背景 |
| `--brand-3` | `#BAE7FF` | 浅色 hover 底 |
| `--brand-4` | `#91D5FF` | 浅色 active 底 |
| `--brand-5` | `#69C0FF` | 浅色图标次级 |
| `--brand-6` | `#40A9FF` | 暗色模式下品牌提亮 / 链接 hover |
| `--brand-7` | `#1890FF` | **主品牌色（基准）**，主按钮、链接、焦点环 |
| `--brand-8` | `#096DD9` | 主按钮 hover |
| `--brand-9` | `#0050B3` | 主按钮 active |
| `--brand-10` | `#003A8C` | 深色文字/强调标题 |

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--accent-1` | `#EEF2FF` | C 端强调浅底纹 |
| `--accent-2` | `#DCE3FF` | C 端浅色 hover 底 |
| `--accent-3` | `#B9C7FF` | C 端浅色图标 |
| `--accent-4` | `#8FA5FF` | C 端次级强调 |
| `--accent-5` | `#6A82FF` | C 端图标强调 |
| `--accent-6` | `#4566FF` | C 端链接 hover |
| `--accent-7` | `#245BFF` | **C 端强调基准（钴蓝）**，封面强调、阅读进度 |
| `--accent-8` | `#1A47D9` | C 端强调 hover |
| `--accent-9` | `#1238A8` | C 端强调 active |
| `--accent-10` | `#0B2470` | C 端深色强调文字 |

> 说明：B 端默认使用 brand；C 端在需要与 B 端区分的强调场景（如阅读器强调、封面装饰）使用 accent。两者共用同一套语义层机制。 [Expert judgment]

### 4.2 功能色（4 类 × 5 级）

每类以 5 级呈现：`-1` 为浅背景，`-2` 为浅色辅助，`-3` 为默认主色，`-4` 为 hover，`-5` 为 active/深色。

#### Success（成功）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--success-1` | `#F6FFED` | 成功底纹背景 |
| `--success-2` | `#B7EB8F` | 成功浅色辅助 |
| `--success-3` | `#52C41A` | **成功主色** |
| `--success-4` | `#389E0D` | 成功 hover |
| `--success-5` | `#237804` | 成功 active / 深色 |

#### Warning（警告）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--warning-1` | `#FFFBE6` | 警告底纹背景 |
| `--warning-2` | `#FFE58F` | 警告浅色辅助 |
| `--warning-3` | `#FAAD14` | **警告主色** |
| `--warning-4` | `#D48806` | 警告 hover |
| `--warning-5` | `#AD6800` | 警告 active / 深色 |

#### Error（错误）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--error-1` | `#FFF1F0` | 错误底纹背景 |
| `--error-2` | `#FFA39E` | 错误浅色辅助 |
| `--error-3` | `#F5222D` | **错误主色** |
| `--error-4` | `#CF1322` | 错误 hover |
| `--error-5` | `#A8071A` | 错误 active / 深色 |

#### Info（信息）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--info-1` | `#E6F7FF` | 信息底纹背景 |
| `--info-2` | `#91D5FF` | 信息浅色辅助 |
| `--info-3` | `#1890FF` | **信息主色**（与 brand-7 同源） |
| `--info-4` | `#096DD9` | 信息 hover |
| `--info-5` | `#0050B3` | 信息 active / 深色 |

### 4.3 中性色（14 级 gray）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--gray-1` | `#F7F7F8` | 页面背景、最浅底 |
| `--gray-2` | `#EDEDF0` | 次级背景、subtle 底 |
| `--gray-3` | `#E2E2E7` | 浅分隔线、subtle 边框 |
| `--gray-4` | `#D3D3DA` | 默认边框、卡片描边 |
| `--gray-5` | `#B8B8C1` | 禁用文字、占位符 |
| `--gray-6` | `#9999A5` | 三级文字、辅助说明 |
| `--gray-7` | `#7A7A86` | 图标次级、次要说明 |
| `--gray-8` | `#5E5E6A` | 二级文字 |
| `--gray-9` | `#45454F` | 正文次级 |
| `--gray-10` | `#2F2F37` | 正文主级（次） |
| `--gray-11` | `#1F1F25` | 深色文字 |
| `--gray-12` | `#15151B` | 标题深色 |
| `--gray-13` | `#0E0E13` | 近黑文字 |
| `--gray-14` | `#0A0B12` | 最深文字 / 暗色页面底 |

### 4.4 语义层映射表（L2）

> 组件必须引用本表令牌，而非上述 L1 原始令牌。

#### 文本（text）

| 语义令牌 | 浅色指向 | 用途 |
| --- | --- | --- |
| `--color-text-primary` | `var(--gray-14)` | 主标题、正文主文字 |
| `--color-text-secondary` | `var(--gray-8)` | 次级说明、辅助文字 |
| `--color-text-tertiary` | `var(--gray-6)` | 三级信息、时间戳 |
| `--color-text-disabled` | `var(--gray-5)` | 禁用态文字 |
| `--color-text-inverse` | `var(--gray-1)` | 深色/品牌底上的文字 |
| `--color-text-link` | `var(--brand-7)` | 链接文字 |
| `--color-text-link-hover` | `var(--brand-6)` | 链接 hover |

#### 背景（bg）

| 语义令牌 | 浅色指向 | 用途 |
| --- | --- | --- |
| `--color-bg-page` | `var(--gray-1)` | 页面底色 |
| `--color-bg-surface` | `#FFFFFF` | 卡片/面板表面 |
| `--color-bg-subtle` | `var(--gray-2)` | 次级底、表头、hover 底 |
| `--color-bg-elevated` | `#FFFFFF` | 浮层（dropdown/popover），配合阴影 |
| `--color-bg-mask` | `rgba(10, 11, 18, 0.45)` | 遮罩层 |

#### 边框（border）

| 语义令牌 | 浅色指向 | 用途 |
| --- | --- | --- |
| `--color-border-default` | `var(--gray-4)` | 默认描边、分隔线 |
| `--color-border-subtle` | `var(--gray-3)` | 弱分隔线 |
| `--color-border-focus` | `var(--brand-7)` | 焦点环（配合 outline-offset） |
| `--color-border-strong` | `var(--gray-6)` | 强调描边（如错误态） |

#### 品牌（brand）

| 语义令牌 | 浅色指向 | 用途 |
| --- | --- | --- |
| `--color-brand` | `var(--brand-7)` | 品牌主色（主按钮、焦点） |
| `--color-brand-hover` | `var(--brand-8)` | 品牌 hover |
| `--color-brand-active` | `var(--brand-9)` | 品牌 active |
| `--color-brand-bg` | `var(--brand-2)` | 品牌浅底（选中 tag、info 提示） |
| `--color-brand-border` | `var(--brand-3)` | 品牌浅边框 |
| `--color-accent` | `var(--accent-7)` | C 端强调色 |
| `--color-accent-hover` | `var(--accent-8)` | C 端强调 hover |
| `--color-accent-active` | `var(--accent-9)` | C 端强调 active |

#### 反馈（feedback）

| 语义令牌 | 浅色指向 | 用途 |
| --- | --- | --- |
| `--color-feedback-success` | `var(--success-3)` | 成功图标/文字 |
| `--color-feedback-success-bg` | `var(--success-1)` | 成功提示底 |
| `--color-feedback-warning` | `var(--warning-3)` | 警告图标/文字 |
| `--color-feedback-warning-bg` | `var(--warning-1)` | 警告提示底 |
| `--color-feedback-error` | `var(--error-3)` | 错误图标/文字/边框 |
| `--color-feedback-error-bg` | `var(--error-1)` | 错误提示底 |
| `--color-feedback-info` | `var(--info-3)` | 信息图标/文字 |
| `--color-feedback-info-bg` | `var(--info-1)` | 信息提示底 |

### 4.5 使用铁律

1. **组件代码禁止裸色值**：任何 `color`、`background`、`border-color` 必须引用 L2 语义令牌。 [Expert judgment]
2. **禁止直接引用 L1**：`var(--brand-7)` 不应出现在业务组件中，应使用 `var(--color-brand)`。
3. **强调色克制**：同一视图内品牌色面积建议 ≤ 15%，避免视觉过载。 [Expert judgment]
4. **状态成套使用**：反馈色与对应 bg 成对出现（如 error + error-bg），不混搭其他色阶。
5. **可访问性兜底**：文字与其背景对比度需满足第 14 章要求；不达标时优先调整语义指向，而非临时改色。 [Research-backed]

---

## 5. 字体排印

### 5.1 字体族（font-family）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--font-sans` | `'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', system-ui, -apple-system, sans-serif` | UI 界面通用字体 |
| `--font-serif` | `'Noto Serif SC', 'Source Han Serif SC', 'Songti SC', serif` | 小说正文、标题展示 |
| `--font-mono` | `'JetBrains Mono', 'SFMono-Regular', Consolas, 'Liberation Mono', monospace` | 代码、数据、等宽场景 |

> Noto Sans/Serif SC 选择原因：开源、跨平台一致、CJK 覆盖完整，避免 PingFang 在非 Apple 设备缺失导致的回退抖动。 [Research-backed]

### 5.2 字号阶梯（font-size）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--font-size-display` | `64px` | 营销/封面大标题 |
| `--font-size-h1` | `40px` | 页面主标题 |
| `--font-size-h2` | `28px` | 区块标题 |
| `--font-size-h3` | `20px` | 卡片/小节标题 |
| `--font-size-body` | `16px` | 正文（UI 默认） |
| `--font-size-caption` | `13px` | 辅助文字、标签、元信息 |
| `--font-size-overline` | `11px` | 行内标签、表头小字 |

### 5.3 字重（font-weight）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--font-weight-regular` | `400` | 正文默认 |
| `--font-weight-medium` | `500` | 次级强调、按钮文字 |
| `--font-weight-semibold` | `600` | 标题、卡片标题 |
| `--font-weight-bold` | `700` | 强调标题、Display |

### 5.4 行高（line-height）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--line-height-tight` | `1.15` | 标题、Display |
| `--line-height-normal` | `1.5` | UI 正文、按钮 |
| `--line-height-relaxed` | `1.7` | 长说明文本 |

### 5.5 小说正文专用

小说阅读场景独立令牌，确保沉浸式阅读体验：

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--novel-font-family` | `var(--font-serif)` | 正文衬线字体 |
| `--novel-font-size` | `18px` | 正文字号（大于 UI 16px，降低阅读疲劳） [Research-backed] |
| `--novel-line-height` | `1.8` | 行高（约 32.4px 行距） |
| `--novel-paragraph-spacing` | `1em` | 段间距（18px） |
| `--novel-text-color` | `var(--gray-11)` | 正文色（不用最深 gray-14，降低对比刺眼） [Expert judgment] |
| `--novel-measure` | `42ch` | 单行最佳字数宽度（约 42 个汉字符） [Research-backed] |

```css
.novel-content {
  font-family: var(--novel-font-family);
  font-size: var(--novel-font-size);
  line-height: var(--novel-line-height);
  color: var(--novel-text-color);
  max-width: var(--novel-measure);
}
.novel-content p {
  margin: 0 0 var(--novel-paragraph-spacing);
}
```

### 5.6 小说阅读主题令牌

小说阅读器提供 4 种阅读主题（日间 / 夜间 / 护眼 / 羊皮纸），每种主题独立定义背景色与文字色，**不使用通用语义令牌**（`--color-bg-page`、`--color-text-primary`），以保证阅读舒适度与主题纯粹性。该组令牌独立于通用 gray 色阶，单独维护 L1 Primitive 与 L2 Semantic 两层；主题切换时仅改 L2 指向，L1 与组件层不动。

#### L1 Primitive · 阅读主题色（独立于通用 gray 色阶）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--read-bg-day` | `#FFFFFF` | 日间主题背景 |
| `--read-bg-night` | `#1A1A1A` | 夜间主题背景 |
| `--read-bg-sepia` | `#F5EDDC` | 护眼主题背景 |
| `--read-bg-parchment` | `#F4ECD8` | 羊皮纸主题背景 |
| `--read-text-day` | `#2A2B33` | 日间主题正文色 |
| `--read-text-night` | `#E0E0E0` | 夜间主题正文色 |
| `--read-text-sepia` | `#5B4636` | 护眼主题正文色 |
| `--read-text-parchment` | `#5B4636` | 羊皮纸主题正文色 |
| `--read-text-secondary-day` | `#5E5E6A` | 日间主题次级文字 |
| `--read-text-secondary-night` | `#888888` | 夜间主题次级文字 |
| `--read-text-secondary-sepia` | `#8C7355` | 护眼主题次级文字 |
| `--read-text-secondary-parchment` | `#8C7355` | 羊皮纸主题次级文字 |

#### L2 Semantic · 阅读主题（切换时仅改这一层）

| 语义令牌 | 默认指向（浅色） | 用途 |
| --- | --- | --- |
| `--novel-read-bg` | `var(--read-bg-day)` | 阅读器画布背景 |
| `--novel-read-text` | `var(--read-text-day)` | 阅读器正文文字 |
| `--novel-read-text-secondary` | `var(--read-text-secondary-day)` | 阅读器次级文字（章节名、时间戳） |

```css
:root {
  /* L1 Primitive · 阅读主题色（独立于通用 gray 色阶） */
  --read-bg-day: #FFFFFF;
  --read-bg-night: #1A1A1A;
  --read-bg-sepia: #F5EDDC;
  --read-bg-parchment: #F4ECD8;

  --read-text-day: #2A2B33;
  --read-text-night: #E0E0E0;
  --read-text-sepia: #5B4636;
  --read-text-parchment: #5B4636;

  --read-text-secondary-day: #5E5E6A;
  --read-text-secondary-night: #888888;
  --read-text-secondary-sepia: #8C7355;
  --read-text-secondary-parchment: #8C7355;

  /* L2 Semantic · 阅读主题（切换时仅改这一层） */
  --novel-read-bg: var(--read-bg-day);
  --novel-read-text: var(--read-text-day);
  --novel-read-text-secondary: var(--read-text-secondary-day);
}

/* 暗色模式：系统暗色时默认指向夜间主题 */
[data-theme='dark'] {
  --novel-read-bg: var(--read-bg-night);
  --novel-read-text: var(--read-text-night);
  --novel-read-text-secondary: var(--read-text-secondary-night);
}
```

> 阅读主题独立于通用色彩体系，原因有二：① 阅读场景对底色色温与文字明度有专门调校（如护眼/羊皮纸的暖黄底），强行映射 gray 色阶会破坏阅读舒适度；② 阅读器主题切换是用户主动选择（4 选 1），与系统浅/暗模式解耦，故 L2 仅在系统暗色时默认指向 night，护眼/羊皮纸由阅读器组件按用户选择即时覆盖 L2。 [Research-backed]

### 5.7 阅读器交互区令牌

小说阅读器的交互区域（翻页点击区、菜单呼出区、工具栏、进度条、章节间距等）需专用令牌定义，以保证触控热区符合人因工程且与正文排版解耦。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--reader-tap-zone` | `80px` | 左右翻页点击区宽度（屏幕左右两侧） |
| `--reader-tap-center` | `40%` | 中间区域（呼出菜单/设置）占屏宽比 |
| `--reader-toolbar-height` | `56px` | 阅读器顶部/底部工具栏高度 |
| `--reader-progress-height` | `3px` | 阅读进度条高度 |
| `--reader-chapter-gap` | `var(--space-12)` | 章节间间距（48px） |
| `--reader-paragraph-indent` | `2em` | 段落首行缩进 |
| `--reader-line-spacing-extra` | `0.05em` | 额外字间距（可选调节） |

```css
:root {
  /* ---------- 阅读器交互区 ---------- */
  --reader-tap-zone: 80px;
  --reader-tap-center: 40%;
  --reader-toolbar-height: 56px;
  --reader-progress-height: 3px;
  --reader-chapter-gap: var(--space-12);
  --reader-paragraph-indent: 2em;
  --reader-line-spacing-extra: 0.05em;
}
```

> 点击热区 80px 与工具栏 56px 参考移动端触控目标最小 44pt（iOS HIG）/ 48dp（Material）基线并向上取整，保证拇指操作舒适；中间区域占屏宽 40% 用于呼出菜单，避免误触翻页。 [Research-backed]

---

## 6. 间距系统

### 6.1 间距令牌（13 档，4px 基准网格）

| 令牌 | 值 | 倍数 |
| --- | --- | --- |
| `--space-1` | `4px` | 1× |
| `--space-2` | `8px` | 2× |
| `--space-3` | `12px` | 3× |
| `--space-4` | `16px` | 4× |
| `--space-5` | `20px` | 5× |
| `--space-6` | `24px` | 6× |
| `--space-8` | `32px` | 8× |
| `--space-10` | `40px` | 10× |
| `--space-12` | `48px` | 12× |
| `--space-16` | `64px` | 16× |
| `--space-20` | `80px` | 20× |
| `--space-24` | `96px` | 24× |
| `--space-32` | `128px` | 32× |

> 命名规则：`space-{n}`，其中 `n = 实际像素 / 4`。仅保留实际使用的档位，跳过 7/9/11 等非约定倍数以降低选择成本。 [Expert judgment]

### 6.2 场景推荐

| 场景 | 推荐令牌 | 说明 |
| --- | --- | --- |
| 图标与文字间距 | `--space-2`（8px） | 行内图标贴文字 |
| 组件内边距（紧凑，如 tag） | `--space-1` ~ `--space-2` | 4–8px |
| 组件内边距（按钮/输入框） | `--space-2` ~ `--space-3` | 8–12px |
| 卡片内边距 | `--space-4` ~ `--space-6` | 16–24px |
| 卡片之间间距 | `--space-4` ~ `--space-6` | 16–24px |
| 区块间距（section 间） | `--space-8` ~ `--space-12` | 32–48px |
| 章节间距（大段落分隔） | `--space-16` ~ `--space-24` | 64–96px |
| 页面顶/底留白 | `--space-20` ~ `--space-32` | 80–128px |

---

## 7. 圆角系统

### 7.1 圆角令牌（8 档）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--radius-none` | `0` | 表格行、分隔条、全宽图 |
| `--radius-xs` | `2px` | tag、小徽标、输入框内嵌 |
| `--radius-sm` | `4px` | 按钮（紧凑）、小卡片、checkbox |
| `--radius-md` | `8px` | 默认按钮、输入框、卡片 |
| `--radius-lg` | `12px` | 大卡片、面板、modal 头部 |
| `--radius-xl` | `16px` | 大型容器、封面容器 |
| `--radius-2xl` | `24px` | 营销卡片、hero 容器 |
| `--radius-full` | `9999px` | 胶囊、圆形头像、圆形按钮 |

### 7.2 适用场景指引

- **同层一致**：同一层级组件圆角保持一致（如所有默认按钮统一 `--radius-md`）。 [Expert judgment]
- **嵌套递减**：嵌套容器外层圆角 ≥ 内层圆角，避免视觉错位（如 modal `--radius-lg`，其内卡片 `--radius-md`）。 [Research-backed]
- **B 端偏小**：B 端后台偏数据型，推荐 `--radius-sm` ~ `--radius-md`；C 端偏内容型，可上探至 `--radius-lg` ~ `--radius-xl`。 [Expert judgment]

---

## 8. 阴影与层级

### 8.1 阴影令牌（5 级，含完整 box-shadow）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--sh-1` | `0 1px 2px 0 rgba(0, 0, 0, 0.04)` | 极弱阴影，卡片浮起 hint |
| `--sh-2` | `0 2px 8px 0 rgba(0, 0, 0, 0.08)` | 卡片默认阴影 |
| `--sh-3` | `0 4px 12px 0 rgba(0, 0, 0, 0.10)` | dropdown、悬浮菜单 |
| `--sh-4` | `0 8px 24px 0 rgba(0, 0, 0, 0.12)` | drawer、popover |
| `--sh-5` | `0 16px 48px 0 rgba(0, 0, 0, 0.16)` | modal、全屏弹层 |

```css
:root {
  --sh-1: 0 1px 2px 0 rgba(0, 0, 0, 0.04);
  --sh-2: 0 2px 8px 0 rgba(0, 0, 0, 0.08);
  --sh-3: 0 4px 12px 0 rgba(0, 0, 0, 0.10);
  --sh-4: 0 8px 24px 0 rgba(0, 0, 0, 0.12);
  --sh-5: 0 16px 48px 0 rgba(0, 0, 0, 0.16);
}
```

### 8.2 暗色模式阴影调整

暗色模式下浅色阴影不可见，需调整策略 [Research-backed]：

- **降低阴影不透明度**：暗色模式阴影 `rgba(0,0,0,x)` 提升至约 1.5–2 倍，或改用深色叠加 + 1px 顶部高光描边。
- **优先用边框分层**：暗色下浮层与背景的分离更多依赖 `1px solid var(--color-border-subtle)` + 弱阴影，而非纯阴影。
- **避免纯黑阴影**：纯 `#000` 阴影在暗底上无层次，使用 `rgba(0,0,0,0.5)` 以上并配合 elevation 边框。

暗色阴影覆盖示例见第 12 章。

---

## 9. 栅格布局

### 9.1 12 列栅格 + 断点定义

采用 12 列栅格，5 个断点：

| 断点令牌 | 最小宽度 | 列数 | 槽宽（gutter） | 边距（margin） | 典型设备 |
| --- | --- | --- | --- | --- | --- |
| `xs` | `0` | 4 列 | `16px` | `16px` | 手机竖屏 |
| `sm` | `576px` | 4 列 | `16px` | `24px` | 手机横屏 / 小平板 |
| `md` | `768px` | 8 列 | `24px` | `32px` | 平板 |
| `lg` | `992px` | 12 列 | `24px` | `48px` | 小桌面 |
| `xl` | `1200px` | 12 列 | `32px` | `auto（居中）` | 大桌面 |

### 9.2 常用列组合

```
12 列栅格常用组合（lg/xl）

┌─────────────────────────────────────────────┐ 全宽 12
├───────────────┬─────────────────────────────┤ 6 + 6
├─────────┬───────────────────────────────────┤ 4 + 8
├───────────────────┬─────────────────────────┤ 8 + 4
├───────┬───────┬─────────────────────────────┤ 3 + 3 + 6
├─────┬─────┬─────┬───────────────────────────┤ 2 + 2 + 2 + 6
├───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┤ 1×12（仅数据表）
```

### 9.3 内容区最大宽度建议

| 场景 | 最大宽度 | 说明 |
| --- | --- | --- |
| 小说阅读正文 | `720px` | 对应 `--novel-measure` 居中，保证单行可读 [Research-backed] |
| C 端内容流 | `1200px` | 列表/详情通用容器 |
| B 端工作台 | `1440px` | 后台多栏布局 |
| 全宽营销页 | `1600px` | hero、大图 |
| 表单/窄内容 | `480px` | 登录、单列表单 |

```css
.container { width: 100%; margin-inline: auto; padding-inline: var(--space-4); }
.container--novel  { max-width: 720px; }
.container--read   { max-width: 1200px; }
.container--admin  { max-width: 1440px; }
@media (min-width: 768px)  { .container { padding-inline: var(--space-6); } }
@media (min-width: 1200px) { .container { padding-inline: var(--space-12); } }
```

---

## 10. 动效规范

### 10.1 缓动曲线（4 条）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--ease-standard` | `cubic-bezier(0.4, 0, 0.2, 1)` | 标准缓动，通用进入/退出 |
| `--ease-emphasized` | `cubic-bezier(0.2, 0, 0, 1)` | 强调进入（减速），如 modal 展开 |
| `--ease-decelerate` | `cubic-bezier(0, 0, 0.2, 1)` | 减速进入，元素出现 |
| `--ease-accelerate` | `cubic-bezier(0.4, 0, 1, 1)` | 加速退出，元素消失 |

### 10.2 时长（5 档）

| 令牌 | 值 | 用途 | 感知 |
| --- | --- | --- | --- |
| `--dur-instant` | `90ms` | 微反馈（hover、ripple） | 即时（Doherty 阈值内） [Research-backed] |
| `--dur-fast` | `150ms` | 小型切换（toggle、tooltip） | 快速 |
| `--dur-normal` | `240ms` | 常规过渡（dropdown、面板） | 标准 |
| `--dur-slow` | `360ms` | 大型过渡（drawer、collapse） | 明显 |
| `--dur-deliberate` | `540ms` | 强调动画（modal、页面转场） | 刻意 |

> Doherty 阈值：响应 ≤ 100ms 被用户感知为"即时"，故微交互统一 ≤ 90ms。 [Research-backed]

### 10.3 prefers-reduced-motion 要求

必须尊重用户系统级减少动效偏好 [Research-backed]：

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

> 规则：不强制关闭动画语义，但将其压缩至近乎即时；不删除状态变化本身，仅去除位移/缩放过程。

---

## 11. 层级系统（z-index）

### 11.1 层级令牌（6 级）

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--z-index-base` | `0` | 默认文档流内容 |
| `--z-index-dropdown` | `1000` | 下拉菜单、自动补全 |
| `--z-index-sticky` | `1100` | 吸顶导航、sticky 表头 |
| `--z-index-drawer` | `1200` | 抽屉、侧滑面板 |
| `--z-index-modal` | `1300` | 模态框、对话框 |
| `--z-index-toast` | `1400` | 全局通知、toast（最高） |

### 11.2 层级间距说明

- **步长 100**：相邻层级差 100，留出组件内部子层（如 modal 内 header/body/footer）使用 1–99 的余量。 [Expert judgment]
- **toast 永远最高**：通知需在任何浮层之上可见，固定 1400。
- **遮罩 mask**：跟随其所属浮层，使用 `calc(var(--z-index-modal) - 1)` 等，保证 mask 在浮层之下、其他内容之上。
- **禁止裸 z-index 值**：业务代码不得写 `z-index: 9999`，必须引用令牌。 [Expert judgment]

---

## 12. 暗黑模式架构

### 12.1 切换原理

暗黑模式**只改变 L2 语义令牌的指向**，L1 原始令牌与 L3 组件令牌保持不变。通过 `[data-theme='dark']` 属性选择器在根元素切换，全站自动适配。 [Research-backed]

```
浅色: --color-text-primary → var(--gray-14)   (#0A0B12 深字)
暗色: --color-text-primary → var(--gray-1)    (#F7F7F8 浅字)
```

### 12.2 浅色 / 暗色映射对照表

| 语义令牌 | 浅色值 | 暗色值 | 调整说明 |
| --- | --- | --- | --- |
| `--color-text-primary` | `var(--gray-14)` | `var(--gray-1)` | 深字 → 浅字 |
| `--color-text-secondary` | `var(--gray-8)` | `var(--gray-5)` | 提亮一档 |
| `--color-text-tertiary` | `var(--gray-6)` | `var(--gray-6)` | 居中灰保持 |
| `--color-text-disabled` | `var(--gray-5)` | `var(--gray-7)` | 暗底下降低对比 |
| `--color-text-inverse` | `var(--gray-1)` | `var(--gray-14)` | 反相 |
| `--color-text-link` | `var(--brand-7)` | `var(--brand-6)` | 品牌提亮一档保对比 [Research-backed] |
| `--color-bg-page` | `var(--gray-1)` | `var(--gray-14)` | 浅底 → 深底 |
| `--color-bg-surface` | `#FFFFFF` | `var(--gray-12)` | 白 → 深面板 |
| `--color-bg-subtle` | `var(--gray-2)` | `var(--gray-11)` | 次级底 |
| `--color-bg-elevated` | `#FFFFFF` | `var(--gray-12)` | 浮层底 |
| `--color-bg-mask` | `rgba(10,11,18,0.45)` | `rgba(0,0,0,0.65)` | 暗色加深 |
| `--color-border-default` | `var(--gray-4)` | `var(--gray-10)` | 边框提亮 |
| `--color-border-subtle` | `var(--gray-3)` | `var(--gray-11)` | 弱边框 |
| `--color-border-focus` | `var(--brand-7)` | `var(--brand-6)` | 焦点环提亮 |
| `--color-brand` | `var(--brand-7)` | `var(--brand-6)` | 提亮保对比 |
| `--color-brand-hover` | `var(--brand-8)` | `var(--brand-7)` | hover 跟随 |
| `--color-brand-active` | `var(--brand-9)` | `var(--brand-8)` | active 跟随 |
| `--color-brand-bg` | `var(--brand-2)` | `rgba(24,144,255,0.16)` | 暗色用透明叠加 |
| `--color-feedback-success` | `var(--success-3)` | `var(--success-2)` | 提亮 |
| `--color-feedback-success-bg` | `var(--success-1)` | `rgba(82,196,26,0.16)` | 透明叠加 |
| `--color-feedback-warning` | `var(--warning-3)` | `var(--warning-2)` | 提亮 |
| `--color-feedback-warning-bg` | `var(--warning-1)` | `rgba(250,173,20,0.16)` | 透明叠加 |
| `--color-feedback-error` | `var(--error-3)` | `var(--error-2)` | 提亮 |
| `--color-feedback-error-bg` | `var(--error-1)` | `rgba(245,34,45,0.16)` | 透明叠加 |
| `--color-feedback-info` | `var(--info-3)` | `var(--info-2)` | 提亮 |
| `--color-feedback-info-bg` | `var(--info-1)` | `rgba(24,144,255,0.16)` | 透明叠加 |
| `--sh-1` | `0 1px 2px 0 rgba(0,0,0,0.04)` | `0 1px 2px 0 rgba(0,0,0,0.40)` | 加深 |
| `--sh-2` | `0 2px 8px 0 rgba(0,0,0,0.08)` | `0 2px 8px 0 rgba(0,0,0,0.44)` | 加深 |
| `--sh-3` | `0 4px 12px 0 rgba(0,0,0,0.10)` | `0 4px 12px 0 rgba(0,0,0,0.48)` | 加深 |
| `--sh-4` | `0 8px 24px 0 rgba(0,0,0,0.12)` | `0 8px 24px 0 rgba(0,0,0,0.52)` | 加深 |
| `--sh-5` | `0 16px 48px 0 rgba(0,0,0,0.16)` | `0 16px 48px 0 rgba(0,0,0,0.56)` | 加深 |

### 12.3 CSS 实现示例

```css
/* 主题切换入口 */
:root { color-scheme: light; }
[data-theme='dark'] { color-scheme: dark; }

/* 暗色覆盖：仅重写 L2 语义层 */
[data-theme='dark'] {
  --color-text-primary: var(--gray-1);
  --color-text-secondary: var(--gray-5);
  --color-text-link: var(--brand-6);
  --color-bg-page: var(--gray-14);
  --color-bg-surface: var(--gray-12);
  --color-bg-subtle: var(--gray-11);
  --color-bg-elevated: var(--gray-12);
  --color-border-default: var(--gray-10);
  --color-border-focus: var(--brand-6);
  --color-brand: var(--brand-6);
  --color-brand-hover: var(--brand-7);
  --color-brand-active: var(--brand-8);
  --color-brand-bg: rgba(24, 144, 255, 0.16);
  --color-feedback-success: var(--success-2);
  --color-feedback-success-bg: rgba(82, 196, 26, 0.16);
  --color-feedback-error: var(--error-2);
  --color-feedback-error-bg: rgba(245, 34, 45, 0.16);

  /* 阅读主题：系统暗色时默认指向夜间主题 */
  --novel-read-bg: var(--read-bg-night);
  --novel-read-text: var(--read-text-night);
  --novel-read-text-secondary: var(--read-text-secondary-night);

  --sh-1: 0 1px 2px 0 rgba(0, 0, 0, 0.40);
  --sh-5: 0 16px 48px 0 rgba(0, 0, 0, 0.56);
}
```

---

## 13. 令牌输出格式

### 13.1 CSS 自定义属性完整输出

```css
/* ============================================================
 * 前端底层设计规范 · Design Tokens · CSS Output
 * Version: V1.0.0 · 2026.07
 * ============================================================ */

:root {
  /* ---------- L1 Primitive · 品牌 ---------- */
  --brand-1: #F0F9FF;
  --brand-2: #E6F7FF;
  --brand-3: #BAE7FF;
  --brand-4: #91D5FF;
  --brand-5: #69C0FF;
  --brand-6: #40A9FF;
  --brand-7: #1890FF;
  --brand-8: #096DD9;
  --brand-9: #0050B3;
  --brand-10: #003A8C;

  /* ---------- L1 Primitive · 强调（accent） ---------- */
  --accent-1: #EEF2FF;
  --accent-2: #DCE3FF;
  --accent-3: #B9C7FF;
  --accent-4: #8FA5FF;
  --accent-5: #6A82FF;
  --accent-6: #4566FF;
  --accent-7: #245BFF;
  --accent-8: #1A47D9;
  --accent-9: #1238A8;
  --accent-10: #0B2470;

  /* ---------- L1 Primitive · 功能色 ---------- */
  --success-1: #F6FFED;
  --success-2: #B7EB8F;
  --success-3: #52C41A;
  --success-4: #389E0D;
  --success-5: #237804;

  --warning-1: #FFFBE6;
  --warning-2: #FFE58F;
  --warning-3: #FAAD14;
  --warning-4: #D48806;
  --warning-5: #AD6800;

  --error-1: #FFF1F0;
  --error-2: #FFA39E;
  --error-3: #F5222D;
  --error-4: #CF1322;
  --error-5: #A8071A;

  --info-1: #E6F7FF;
  --info-2: #91D5FF;
  --info-3: #1890FF;
  --info-4: #096DD9;
  --info-5: #0050B3;

  /* ---------- L1 Primitive · 中性色 14 级 ---------- */
  --gray-1: #F7F7F8;
  --gray-2: #EDEDF0;
  --gray-3: #E2E2E7;
  --gray-4: #D3D3DA;
  --gray-5: #B8B8C1;
  --gray-6: #9999A5;
  --gray-7: #7A7A86;
  --gray-8: #5E5E6A;
  --gray-9: #45454F;
  --gray-10: #2F2F37;
  --gray-11: #1F1F25;
  --gray-12: #15151B;
  --gray-13: #0E0E13;
  --gray-14: #0A0B12;

  /* ---------- L2 Semantic · 文本 ---------- */
  --color-text-primary: var(--gray-14);
  --color-text-secondary: var(--gray-8);
  --color-text-tertiary: var(--gray-6);
  --color-text-disabled: var(--gray-5);
  --color-text-inverse: var(--gray-1);
  --color-text-link: var(--brand-7);
  --color-text-link-hover: var(--brand-6);

  /* ---------- L2 Semantic · 背景 ---------- */
  --color-bg-page: var(--gray-1);
  --color-bg-surface: #FFFFFF;
  --color-bg-subtle: var(--gray-2);
  --color-bg-elevated: #FFFFFF;
  --color-bg-mask: rgba(10, 11, 18, 0.45);

  /* ---------- L2 Semantic · 边框 ---------- */
  --color-border-default: var(--gray-4);
  --color-border-subtle: var(--gray-3);
  --color-border-focus: var(--brand-7);
  --color-border-strong: var(--gray-6);

  /* ---------- L2 Semantic · 品牌 ---------- */
  --color-brand: var(--brand-7);
  --color-brand-hover: var(--brand-8);
  --color-brand-active: var(--brand-9);
  --color-brand-bg: var(--brand-2);
  --color-brand-border: var(--brand-3);
  --color-accent: var(--accent-7);
  --color-accent-hover: var(--accent-8);
  --color-accent-active: var(--accent-9);

  /* ---------- L2 Semantic · 反馈 ---------- */
  --color-feedback-success: var(--success-3);
  --color-feedback-success-bg: var(--success-1);
  --color-feedback-warning: var(--warning-3);
  --color-feedback-warning-bg: var(--warning-1);
  --color-feedback-error: var(--error-3);
  --color-feedback-error-bg: var(--error-1);
  --color-feedback-info: var(--info-3);
  --color-feedback-info-bg: var(--info-1);

  /* ---------- 字体 ---------- */
  --font-sans: 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', system-ui, -apple-system, sans-serif;
  --font-serif: 'Noto Serif SC', 'Source Han Serif SC', 'Songti SC', serif;
  --font-mono: 'JetBrains Mono', 'SFMono-Regular', Consolas, 'Liberation Mono', monospace;

  --font-size-display: 64px;
  --font-size-h1: 40px;
  --font-size-h2: 28px;
  --font-size-h3: 20px;
  --font-size-body: 16px;
  --font-size-caption: 13px;
  --font-size-overline: 11px;

  --font-weight-regular: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  --line-height-tight: 1.15;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.7;

  /* ---------- 小说正文 ---------- */
  --novel-font-family: var(--font-serif);
  --novel-font-size: 18px;
  --novel-line-height: 1.8;
  --novel-paragraph-spacing: 1em;
  --novel-text-color: var(--gray-11);
  --novel-measure: 42ch;

  /* ---------- 阅读主题（L1 Primitive + L2 Semantic） ---------- */
  /* L1 Primitive · 阅读主题色（独立于通用 gray 色阶） */
  --read-bg-day: #FFFFFF;
  --read-bg-night: #1A1A1A;
  --read-bg-sepia: #F5EDDC;
  --read-bg-parchment: #F4ECD8;

  --read-text-day: #2A2B33;
  --read-text-night: #E0E0E0;
  --read-text-sepia: #5B4636;
  --read-text-parchment: #5B4636;

  --read-text-secondary-day: #5E5E6A;
  --read-text-secondary-night: #888888;
  --read-text-secondary-sepia: #8C7355;
  --read-text-secondary-parchment: #8C7355;

  /* L2 Semantic · 阅读主题（切换时仅改这一层） */
  --novel-read-bg: var(--read-bg-day);
  --novel-read-text: var(--read-text-day);
  --novel-read-text-secondary: var(--read-text-secondary-day);

  /* ---------- 阅读器交互区 ---------- */
  --reader-tap-zone: 80px;
  --reader-tap-center: 40%;
  --reader-toolbar-height: 56px;
  --reader-progress-height: 3px;
  --reader-chapter-gap: var(--space-12);
  --reader-paragraph-indent: 2em;
  --reader-line-spacing-extra: 0.05em;

  /* ---------- 间距（13 档，4px 基准） ---------- */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
  --space-20: 80px;
  --space-24: 96px;
  --space-32: 128px;

  /* ---------- 圆角（8 档） ---------- */
  --radius-none: 0;
  --radius-xs: 2px;
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-2xl: 24px;
  --radius-full: 9999px;

  /* ---------- 阴影（5 级） ---------- */
  --sh-1: 0 1px 2px 0 rgba(0, 0, 0, 0.04);
  --sh-2: 0 2px 8px 0 rgba(0, 0, 0, 0.08);
  --sh-3: 0 4px 12px 0 rgba(0, 0, 0, 0.10);
  --sh-4: 0 8px 24px 0 rgba(0, 0, 0, 0.12);
  --sh-5: 0 16px 48px 0 rgba(0, 0, 0, 0.16);

  /* ---------- 动效 ---------- */
  --ease-standard: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-emphasized: cubic-bezier(0.2, 0, 0, 1);
  --ease-decelerate: cubic-bezier(0, 0, 0.2, 1);
  --ease-accelerate: cubic-bezier(0.4, 0, 1, 1);

  --dur-instant: 90ms;
  --dur-fast: 150ms;
  --dur-normal: 240ms;
  --dur-slow: 360ms;
  --dur-deliberate: 540ms;

  /* ---------- 层级 z-index（6 级） ---------- */
  --z-index-base: 0;
  --z-index-dropdown: 1000;
  --z-index-sticky: 1100;
  --z-index-drawer: 1200;
  --z-index-modal: 1300;
  --z-index-toast: 1400;

  /* ---------- 图标尺寸 ---------- */
  --icon-size-xs: 12px;
  --icon-size-sm: 14px;
  --icon-size-md: 16px;
  --icon-size-lg: 20px;
  --icon-size-xl: 24px;
  --icon-size-2xl: 32px;

  color-scheme: light;
}

/* ============================================================
 * Dark Theme · 仅覆盖 L2 语义层与阴影
 * ============================================================ */
[data-theme='dark'] {
  --color-text-primary: var(--gray-1);
  --color-text-secondary: var(--gray-5);
  --color-text-tertiary: var(--gray-6);
  --color-text-disabled: var(--gray-7);
  --color-text-inverse: var(--gray-14);
  --color-text-link: var(--brand-6);
  --color-text-link-hover: var(--brand-5);

  --color-bg-page: var(--gray-14);
  --color-bg-surface: var(--gray-12);
  --color-bg-subtle: var(--gray-11);
  --color-bg-elevated: var(--gray-12);
  --color-bg-mask: rgba(0, 0, 0, 0.65);

  --color-border-default: var(--gray-10);
  --color-border-subtle: var(--gray-11);
  --color-border-focus: var(--brand-6);
  --color-border-strong: var(--gray-8);

  --color-brand: var(--brand-6);
  --color-brand-hover: var(--brand-7);
  --color-brand-active: var(--brand-8);
  --color-brand-bg: rgba(24, 144, 255, 0.16);
  --color-brand-border: rgba(24, 144, 255, 0.32);
  --color-accent: var(--accent-6);
  --color-accent-hover: var(--accent-7);
  --color-accent-active: var(--accent-8);

  --color-feedback-success: var(--success-2);
  --color-feedback-success-bg: rgba(82, 196, 26, 0.16);
  --color-feedback-warning: var(--warning-2);
  --color-feedback-warning-bg: rgba(250, 173, 20, 0.16);
  --color-feedback-error: var(--error-2);
  --color-feedback-error-bg: rgba(245, 34, 45, 0.16);
  --color-feedback-info: var(--info-2);
  --color-feedback-info-bg: rgba(24, 144, 255, 0.16);

  --novel-text-color: var(--gray-3);

  /* 阅读主题：系统暗色时默认指向夜间主题 */
  --novel-read-bg: var(--read-bg-night);
  --novel-read-text: var(--read-text-night);
  --novel-read-text-secondary: var(--read-text-secondary-night);

  --sh-1: 0 1px 2px 0 rgba(0, 0, 0, 0.40);
  --sh-2: 0 2px 8px 0 rgba(0, 0, 0, 0.44);
  --sh-3: 0 4px 12px 0 rgba(0, 0, 0, 0.48);
  --sh-4: 0 8px 24px 0 rgba(0, 0, 0, 0.52);
  --sh-5: 0 16px 48px 0 rgba(0, 0, 0, 0.56);

  color-scheme: dark;
}
```

### 13.2 JavaScript / TypeScript 令牌对象完整输出

> 与 CSS 完全对齐：L1 原始令牌以原值输出，L2 语义令牌以 L1 引用键输出（运行时由 CSS var 解析；JS 侧仅做映射记录与类型约束）。

```typescript
/* ============================================================
 * 前端底层设计规范 · Design Tokens · JS/TS Output
 * 与 CSS 自定义属性逐一对齐 · Version: V1.0.0 · 2026.07
 * ============================================================ */

export const tokens = {
  color: {
    brand: {
      brand1: '#F0F9FF',
      brand2: '#E6F7FF',
      brand3: '#BAE7FF',
      brand4: '#91D5FF',
      brand5: '#69C0FF',
      brand6: '#40A9FF',
      brand7: '#1890FF',
      brand8: '#096DD9',
      brand9: '#0050B3',
      brand10: '#003A8C',
    },
    accent: {
      accent1: '#EEF2FF',
      accent2: '#DCE3FF',
      accent3: '#B9C7FF',
      accent4: '#8FA5FF',
      accent5: '#6A82FF',
      accent6: '#4566FF',
      accent7: '#245BFF',
      accent8: '#1A47D9',
      accent9: '#1238A8',
      accent10: '#0B2470',
    },
    success: {
      success1: '#F6FFED',
      success2: '#B7EB8F',
      success3: '#52C41A',
      success4: '#389E0D',
      success5: '#237804',
    },
    warning: {
      warning1: '#FFFBE6',
      warning2: '#FFE58F',
      warning3: '#FAAD14',
      warning4: '#D48806',
      warning5: '#AD6800',
    },
    error: {
      error1: '#FFF1F0',
      error2: '#FFA39E',
      error3: '#F5222D',
      error4: '#CF1322',
      error5: '#A8071A',
    },
    info: {
      info1: '#E6F7FF',
      info2: '#91D5FF',
      info3: '#1890FF',
      info4: '#096DD9',
      info5: '#0050B3',
    },
    gray: {
      gray1: '#F7F7F8',
      gray2: '#EDEDF0',
      gray3: '#E2E2E7',
      gray4: '#D3D3DA',
      gray5: '#B8B8C1',
      gray6: '#9999A5',
      gray7: '#7A7A86',
      gray8: '#5E5E6A',
      gray9: '#45454F',
      gray10: '#2F2F37',
      gray11: '#1F1F25',
      gray12: '#15151B',
      gray13: '#0E0E13',
      gray14: '#0A0B12',
    },
    read: {
      bgDay: '#FFFFFF',
      bgNight: '#1A1A1A',
      bgSepia: '#F5EDDC',
      bgParchment: '#F4ECD8',
      textDay: '#2A2B33',
      textNight: '#E0E0E0',
      textSepia: '#5B4636',
      textParchment: '#5B4636',
      textSecondaryDay: '#5E5E6A',
      textSecondaryNight: '#888888',
      textSecondarySepia: '#8C7355',
      textSecondaryParchment: '#8C7355',
    },
    semantic: {
      textPrimary: 'var(--gray-14)',
      textSecondary: 'var(--gray-8)',
      textTertiary: 'var(--gray-6)',
      textDisabled: 'var(--gray-5)',
      textInverse: 'var(--gray-1)',
      textLink: 'var(--brand-7)',
      textLinkHover: 'var(--brand-6)',
      bgPage: 'var(--gray-1)',
      bgSurface: '#FFFFFF',
      bgSubtle: 'var(--gray-2)',
      bgElevated: '#FFFFFF',
      bgMask: 'rgba(10, 11, 18, 0.45)',
      borderDefault: 'var(--gray-4)',
      borderSubtle: 'var(--gray-3)',
      borderFocus: 'var(--brand-7)',
      borderStrong: 'var(--gray-6)',
      brand: 'var(--brand-7)',
      brandHover: 'var(--brand-8)',
      brandActive: 'var(--brand-9)',
      brandBg: 'var(--brand-2)',
      brandBorder: 'var(--brand-3)',
      accent: 'var(--accent-7)',
      accentHover: 'var(--accent-8)',
      accentActive: 'var(--accent-9)',
      feedbackSuccess: 'var(--success-3)',
      feedbackSuccessBg: 'var(--success-1)',
      feedbackWarning: 'var(--warning-3)',
      feedbackWarningBg: 'var(--warning-1)',
      feedbackError: 'var(--error-3)',
      feedbackErrorBg: 'var(--error-1)',
      feedbackInfo: 'var(--info-3)',
      feedbackInfoBg: 'var(--info-1)',
    },
  },
  font: {
    sans: "'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', system-ui, -apple-system, sans-serif",
    serif: "'Noto Serif SC', 'Source Han Serif SC', 'Songti SC', serif",
    mono: "'JetBrains Mono', 'SFMono-Regular', Consolas, 'Liberation Mono', monospace",
    size: {
      display: '64px',
      h1: '40px',
      h2: '28px',
      h3: '20px',
      body: '16px',
      caption: '13px',
      overline: '11px',
    },
    weight: {
      regular: '400',
      medium: '500',
      semibold: '600',
      bold: '700',
    },
    lineHeight: {
      tight: '1.15',
      normal: '1.5',
      relaxed: '1.7',
    },
  },
  novel: {
    fontFamily: 'var(--font-serif)',
    fontSize: '18px',
    lineHeight: '1.8',
    paragraphSpacing: '1em',
    textColor: 'var(--gray-11)',
    measure: '42ch',
    readBg: 'var(--read-bg-day)',
    readText: 'var(--read-text-day)',
    readTextSecondary: 'var(--read-text-secondary-day)',
  },
  reader: {
    tapZone: '80px',
    tapCenter: '40%',
    toolbarHeight: '56px',
    progressHeight: '3px',
    chapterGap: 'var(--space-12)',
    paragraphIndent: '2em',
    lineSpacingExtra: '0.05em',
  },
  space: {
    space1: '4px',
    space2: '8px',
    space3: '12px',
    space4: '16px',
    space5: '20px',
    space6: '24px',
    space8: '32px',
    space10: '40px',
    space12: '48px',
    space16: '64px',
    space20: '80px',
    space24: '96px',
    space32: '128px',
  },
  radius: {
    none: '0',
    xs: '2px',
    sm: '4px',
    md: '8px',
    lg: '12px',
    xl: '16px',
    xxl: '24px',
    full: '9999px',
  },
  shadow: {
    sh1: '0 1px 2px 0 rgba(0, 0, 0, 0.04)',
    sh2: '0 2px 8px 0 rgba(0, 0, 0, 0.08)',
    sh3: '0 4px 12px 0 rgba(0, 0, 0, 0.10)',
    sh4: '0 8px 24px 0 rgba(0, 0, 0, 0.12)',
    sh5: '0 16px 48px 0 rgba(0, 0, 0, 0.16)',
  },
  motion: {
    ease: {
      standard: 'cubic-bezier(0.4, 0, 0.2, 1)',
      emphasized: 'cubic-bezier(0.2, 0, 0, 1)',
      decelerate: 'cubic-bezier(0, 0, 0.2, 1)',
      accelerate: 'cubic-bezier(0.4, 0, 1, 1)',
    },
    duration: {
      instant: '90ms',
      fast: '150ms',
      normal: '240ms',
      slow: '360ms',
      deliberate: '540ms',
    },
  },
  zIndex: {
    base: '0',
    dropdown: '1000',
    sticky: '1100',
    drawer: '1200',
    modal: '1300',
    toast: '1400',
  },
  iconSize: {
    xs: '12px',
    sm: '14px',
    md: '16px',
    lg: '20px',
    xl: '24px',
    xxl: '32px',
  },
} as const;

export type Tokens = typeof tokens;
```

### 13.3 W3C DTCG JSON 格式示例

```json
{
  "$description": "前端底层设计规范 · Design Tokens · W3C DTCG 2025.10",
  "$schema": "https://design-tokens.org/schema.json",
  "color": {
    "brand": {
      "brand-7": {
        "$type": "color",
        "$value": "#1890FF",
        "$description": "主品牌色基准，对应 Ant Design blue-6"
      },
      "brand-8": { "$type": "color", "$value": "#096DD9", "$description": "品牌 hover" }
    },
    "semantic": {
      "text-primary": {
        "$type": "color",
        "$value": "{color.gray.gray-14}",
        "$description": "主标题与正文主文字"
      },
      "brand": {
        "$type": "color",
        "$value": "{color.brand.brand-7}",
        "$description": "品牌主色语义引用"
      }
    },
    "gray": {
      "gray-14": { "$type": "color", "$value": "#0A0B12" }
    }
  },
  "dimension": {
    "space": {
      "space-4": { "$type": "dimension", "$value": "16px" }
    },
    "radius": {
      "radius-md": { "$type": "dimension", "$value": "8px" }
    }
  },
  "shadow": {
    "sh-2": {
      "$type": "shadow",
      "$value": "0 2px 8px 0 rgba(0, 0, 0, 0.08)"
    }
  }
}
```

> DTCG 要点：每个令牌必含 `$type` 与 `$value`；引用用 `{group.token}` 路径语法；`$description` 可选但推荐。 [Research-backed]

### 13.4 Tailwind 配置映射说明

将令牌映射到 Tailwind theme，令 CSS 工具类直接消费语义令牌：

```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss';

export default {
  content: ['./src/**/*.{ts,tsx,html}'],
  darkMode: ['selector', "[data-theme='dark']"],
  theme: {
    extend: {
      colors: {
        // 语义色 → 引用 CSS 变量，自动跟随主题
        brand: 'var(--color-brand)',
        'brand-hover': 'var(--color-brand-hover)',
        'brand-active': 'var(--color-brand-active)',
        'brand-bg': 'var(--color-brand-bg)',
        accent: 'var(--color-accent)',
        bg: {
          page: 'var(--color-bg-page)',
          surface: 'var(--color-bg-surface)',
          subtle: 'var(--color-bg-subtle)',
          elevated: 'var(--color-bg-elevated)',
        },
        text: {
          primary: 'var(--color-text-primary)',
          secondary: 'var(--color-text-secondary)',
          tertiary: 'var(--color-text-tertiary)',
          disabled: 'var(--color-text-disabled)',
          inverse: 'var(--color-text-inverse)',
          link: 'var(--color-text-link)',
        },
        border: {
          default: 'var(--color-border-default)',
          subtle: 'var(--color-border-subtle)',
          focus: 'var(--color-border-focus)',
        },
        success: 'var(--color-feedback-success)',
        warning: 'var(--color-feedback-warning)',
        error: 'var(--color-feedback-error)',
        info: 'var(--color-feedback-info)',
      },
      fontFamily: {
        sans: 'var(--font-sans)',
        serif: 'var(--font-serif)',
        mono: 'var(--font-mono)',
      },
      fontSize: {
        display: '64px', h1: '40px', h2: '28px', h3: '20px',
        body: '16px', caption: '13px', overline: '11px',
      },
      spacing: {
        1: 'var(--space-1)', 2: 'var(--space-2)', 3: 'var(--space-3)',
        4: 'var(--space-4)', 5: 'var(--space-5)', 6: 'var(--space-6)',
        8: 'var(--space-8)', 10: 'var(--space-10)', 12: 'var(--space-12)',
        16: 'var(--space-16)', 20: 'var(--space-20)', 24: 'var(--space-24)',
        32: 'var(--space-32)',
      },
      borderRadius: {
        none: 'var(--radius-none)', xs: 'var(--radius-xs)', sm: 'var(--radius-sm)',
        md: 'var(--radius-md)', lg: 'var(--radius-lg)', xl: 'var(--radius-xl)',
        '2xl': 'var(--radius-2xl)', full: 'var(--radius-full)',
      },
      boxShadow: {
        1: 'var(--sh-1)', 2: 'var(--sh-2)', 3: 'var(--sh-3)',
        4: 'var(--sh-4)', 5: 'var(--sh-5)',
      },
      transitionDuration: {
        instant: '90ms', fast: '150ms', normal: '240ms',
        slow: '360ms', deliberate: '540ms',
      },
      transitionTimingFunction: {
        standard: 'var(--ease-standard)',
        emphasized: 'var(--ease-emphasized)',
        decelerate: 'var(--ease-decelerate)',
        accelerate: 'var(--ease-accelerate)',
      },
      zIndex: {
        base: 'var(--z-index-base)', dropdown: 'var(--z-index-dropdown)',
        sticky: 'var(--z-index-sticky)', drawer: 'var(--z-index-drawer)',
        modal: 'var(--z-index-modal)', toast: 'var(--z-index-toast)',
      },
    },
  },
} satisfies Config;
```

### 13.5 JS camelCase 与 kebab-case 转换规则

CSS 使用 `kebab-case`，JS/TS 对象键使用 `camelCase`。统一转换规则如下 [Expert judgment]：

| CSS 令牌（kebab-case） | JS 键（camelCase） | 规则 |
| --- | --- | --- |
| `--brand-7` | `brand7` | 去前缀 `--`，数字直接拼接 |
| `--color-text-primary` | `textPrimary` | 去命名空间 `color.`，后续段驼峰 |
| `--space-4` | `space4` | 去前缀，数字拼接 |
| `--radius-md` | `md`（radius 命名空间内） | 去命名空间，保留词 |
| `--radius-2xl` | `xxl` | 数字 + 字母组合，数字转双字母映射（2xl→xxl） |
| `--sh-3` | `sh3` | 去前缀，数字拼接 |
| `--dur-fast` | `fast`（duration 内） | 去命名空间 |
| `--ease-standard` | `standard`（ease 内） | 去命名空间 |
| `--z-index-modal` | `zIndex` → `modal` | 拆为 `zIndex` 命名空间，键为层名 |
| `--icon-size-md` | `md`（iconSize 内） | 去命名空间 |

**转换算法**（伪代码）：

```typescript
// kebab → camel：分段，非首段首字母大写，纯数字段直接拼接
function kebabToCamel(key: string): string {
  return key
    .replace(/^--/, '')
    .split('-')
    .map((seg, i) => (i === 0 ? seg : seg.charAt(0).toUpperCase() + seg.slice(1)))
    .join('');
}
// --color-text-primary → "colorTextPrimary" → 在 color 命名空间下取 "textPrimary"
// --brand-7            → "brand7"
```

> 反向（camel → kebab）：在每个大写字母前插入 `-` 并转小写；数字前不插入连字符（`brand7` → `brand-7` 需特殊处理：识别尾部数字段补 `-`）。建议在构建脚本中以「令牌表」为单一真值生成两侧映射表，避免手写转换歧义。 [Expert judgment]

---

## 14. 无障碍基础

### 14.1 WCAG 2.2 AA 四项准则

1. **对比度可感知**：正文文字与背景对比度 ≥ 4.5:1；大文字（≥ 24px 或 ≥ 18.66px bold）≥ 3:1；UI 组件及图形边界 ≥ 3:1。 [Research-backed]
2. **键盘可达**：所有交互元素可通过键盘操作，可见焦点环（`--color-border-focus` + `outline-offset: 2px`），Tab 顺序符合阅读顺序，禁止键盘陷阱。 [Research-backed]
3. **语义化结构**：使用语义化 HTML（`header/nav/main/article/section/footer`），标题层级递进不跳级，表单控件关联 `label`，图标按钮提供 `aria-label`。 [Research-backed]
4. **色彩无关**：状态信息不单独依赖颜色传达（如错误态除红色外加图标/文字；必填项除色标外加 `*` 或 `aria-required`）。 [Research-backed]

### 14.2 对比度实例表（浅色模式）

| 前景 | 背景 | 前景色 | 背景色 | 对比度 | 是否达标（AA） |
| --- | --- | --- | --- | --- | --- |
| 正文主文字 | 页面底 | `#0A0B12` (gray-14) | `#F7F7F8` (gray-1) | 17.8:1 | 达标 |
| 次级文字 | 页面底 | `#5E5E6A` (gray-8) | `#F7F7F8` (gray-1) | 6.9:1 | 达标 |
| 三级文字 | 页面底 | `#9999A5` (gray-6) | `#F7F7F8` (gray-1) | 3.5:1 | 仅大文字达标 |
| 禁用文字 | 页面底 | `#B8B8C1` (gray-5) | `#F7F7F8` (gray-1) | 2.3:1 | 禁用态豁免 |
| 反白文字 | 品牌底 | `#F7F7F8` (gray-1) | `#1890FF` (brand-7) | 3.4:1 | 大文字达标（按钮文字 ≥ 18.66px bold 时用） |
| 链接文字 | 页面底 | `#1890FF` (brand-7) | `#F7F7F8` (gray-1) | 3.4:1 | 大文字达标；小字链接建议加下划线 |
| 错误文字 | 错误底 | `#F5222D` (error-3) | `#FFF1F0` (error-1) | 4.0:1 | 接近达标，配图标补强 |
| 成功文字 | 成功底 | `#237804` (success-5) | `#F6FFED` (success-1) | 7.1:1 | 达标 |

> 提示：brand-7 (#1890FF) 在浅底上对比度约 3.4:1，仅满足大文字 AA。小号纯白文字按钮若用 brand-7 作底需配合加粗或下划线，或在需要 4.5:1 的场景改用 brand-8 (#096DD9)。 [Expert judgment]

```css
/* 焦点环标准实现 */
:focus-visible {
  outline: 2px solid var(--color-border-focus);
  outline-offset: 2px;
}
/* 禁用移除 outline，改用 box-shadow 焦点以兼容圆角 */
button:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-bg-surface), 0 0 0 4px var(--color-border-focus); }
```

---

## 15. 命名规范

全文档统一使用 **kebab-case**，令牌命名遵循「类别 + 角色 + 限定」结构 [Expert judgment]。

| 类别 | 命名模式 | 示例 |
| --- | --- | --- |
| 颜色（原始） | `{palette}-{shade}` | `brand-7`、`gray-14`、`success-3` |
| 颜色（语义） | `color-{role}-{state}` | `color-text-primary`、`color-bg-surface`、`color-brand-hover` |
| 间距 | `space-{n}`（n = px/4） | `space-4`（16px）、`space-12`（48px） |
| 圆角 | `radius-{size}` | `radius-md`、`radius-2xl`、`radius-full` |
| 阴影 | `sh-{n}` | `sh-1` ~ `sh-5` |
| 动效时长 | `dur-{speed}` | `dur-fast`、`dur-normal` |
| 动效曲线 | `ease-{name}` | `ease-standard`、`ease-emphasized` |
| 层级 | `z-index-{layer}` | `z-index-modal`、`z-index-toast` |
| 图标尺寸 | `icon-size-{scale}` | `icon-size-md`、`icon-size-xl` |
| 字体族 | `font-{family}` | `font-sans`、`font-serif`、`font-mono` |
| 字号 | `font-size-{role}` | `font-size-h1`、`font-size-body` |
| 字重 | `font-weight-{name}` | `font-weight-medium` |
| 行高 | `line-height-{name}` | `line-height-relaxed` |
| 小说专用 | `novel-{property}` | `novel-font-size`、`novel-measure` |
| 阅读主题 | `read-{property}-{theme}` | `read-bg-day`、`read-text-night` |
| 阅读器交互 | `reader-{element}` | `reader-tap-zone`、`reader-toolbar-height` |

**命名约束**：

1. 全小写，单词以连字符 `-` 分隔，禁止下划线与驼峰（CSS 侧）。 [Expert judgment]
2. 色阶索引为纯数字（`brand-7`），复合尺寸用字母（`radius-2xl`），二者不混用。
3. 语义令牌以 `color-` 前缀限定，避免与原始色 `brand-7` 混淆。
4. 状态后缀固定枚举：`-hover` / `-active` / `-focus` / `-disabled` / `-bg` / `-border`，禁止自创状态词。 [Expert judgment]
5. JS 侧对应键见第 13.5 节转换规则，保持 CSS ↔ JS 双向可生成。

---

> **附录 · 证据标注说明**
> - `[Research-backed]`：基于业界公开标准或研究成果（W3C DTCG、WCAG 2.2、Ant Design 调色板、Material Motion、Doherty 阈值等）。
> - `[Expert judgment]`：基于本系统设计专家的综合判断与裁决，无单一外部出处，但遵循行业通行实践。
>
> **文档版本**：V1.0.0 · 2026.07
> **维护**：前端设计工程组
